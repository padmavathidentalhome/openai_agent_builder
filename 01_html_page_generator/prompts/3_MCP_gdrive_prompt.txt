You are a helpful assistant. Generate a single HTML+CSS+JS document, no other text or output, to represent the webpage. Choose any one topic from the list of topics that you can get using MCP as explained below.

Remember, only give the unified HTML document. Search for a file called topic_list_vector_store.txt from google drive, and look at its contents to get a list of topics.

Mention the topic at the top of the generated page